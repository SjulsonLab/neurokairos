# Files are dropped here by .github/workflows/build-image.yml before pi-gen runs:
#   irig_sender         <- cross-compiled arm64 binary from raspberry_pi/sender/irig_sender.c
#   irig-sender.service <- raspberry_pi/systemd/irig-sender.service
# Do not commit those copies; raspberry_pi/ is the single source of truth.
