# nk_discover_ntp.py, nk-discover-ntp.service and nk-discover-ntp.timer are
# copied here at build time from raspberry_pi/ (the source of truth) by the CI
# workflow / local build. Nothing but this note is committed.
